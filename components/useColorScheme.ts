/**
 * Réeexporte le hook useColorScheme natif de React Native.
 * Permet de détecter le mode clair/sombre sur mobile.
 * @returns 'light' ou 'dark' selon le thème système.
 */
export { useColorScheme } from 'react-native';

